<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".presentation.DetailsFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:layout_weight="1"
            android:background="@drawable/radio_selector_normal"
            android:gravity="center_horizontal"
            android:padding="10dp"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="Name Of Type" />

        <ToggleButton
            android:id="@+id/add_to_favorite"
            android:layout_width="58dp"
            android:layout_height="54dp"
            android:layout_marginTop="13dp"
            android:layout_marginRight="16dp"
            android:background="@drawable/radio_selector"
            android:button="@null"
            android:paddingVertical="15dp"
            android:paddingLeft="18dp"
            android:drawableLeft="@drawable/add_reaction"
            android:drawablePadding="10dp"
            android:gravity="center_vertical"
            android:textColor="@color/black" />


    </LinearLayout>

    <include
        android:id="@+id/person_info"
        layout="@layout/person_info"
        android:visibility="gone" />

    <include
        android:id="@+id/planet_info"
        layout="@layout/planet_info"
        android:visibility="gone" />

    <include
        android:id="@+id/starship_info"
        layout="@layout/starship_info"
        android:visibility="gone" />

</LinearLayout>